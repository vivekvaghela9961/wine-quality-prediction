FROM nginx:alpine

# Remove default nginx static assets
RUN rm -rf /usr/share/nginx/html/*

# Copy the custom frontend files to the nginx html directory
COPY frontend/ /usr/share/nginx/html/

# Expose port 80 (nginx default)
EXPOSE 80

# The default command in nginx:alpine runs nginx
